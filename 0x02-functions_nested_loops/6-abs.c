#include "main.h"
/**
 *  * print_sign - function that prints the sign of a number..
 *   * @n: The input number to check
 *    * Return: int
*/
int print_sign(int n)
{
if (n > 0)
{
	x = x * (-1);
	return (x);
}
else
{
	return (x);
}
}
